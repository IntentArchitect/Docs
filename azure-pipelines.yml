trigger:
  batch: 'true'
  branches:
    include:
    - 'development-4.4x'

variables:
  - group: 'Pipeline Agent Pool Names'  # This pulls in variables from the variable group
  githubToken: $(GITHUB_READONLY)     # This defines the githubToken variable using the value of the GITHUB_READONLY secret

pool:
  name: $(BuildPool.AzureManaged)
  demands:
  - 'ImageOverride -equals ubuntu-latest'

# resources:
#   repositories:
#     - repository: modules-dotnet
#       type: github
#       name: IntentArchitect/Intent.Modules.NET
#       ref: refs/heads/$(Build.SourceBranchName)  # Using the same branch name as the pipeline's branch

steps:

  - checkout: self
    persistCredentials: 'true'
    fetchDepth: '1'

  - pwsh: |
      git clone https://$(GitHubToken)@github.com/jp-seini/Intent.Modules.NET.git
    

  #- checkout: modules-dotnet  # Checkout the branch from the second repo  

